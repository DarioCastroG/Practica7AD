<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <!-- Nombre único de la unidad de persistencia -->
    <persistence-unit name="ProductPersistenceUnit">
        <description>
            Ejemplo de unidad de persistencia para Jakarta Persistence
        </description>

        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <!-- <provider>org.eclipse.persistence.jpa.PersistenceProvider</provider> -->

        <!-- Clases que se van a persistir -->
        <class>es.transportesamartin.domain.Usuario</class>
        <class>es.transportesamartin.domain.Pedido</class>

        <!-- Si no se listan todas las clases, incluir también las detectadas automáticamente -->
        <exclude-unlisted-classes>false</exclude-unlisted-classes>

        <!-- Propiedades de la unidad de persistencia -->
        <properties>
            <!-- Configuración de conexión a base de datos MySQL -->
            <property name="jakarta.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver"/>
            <!-- Cambiar el puerto por 3306 si el MySQL usa ese puerto-->
            <property name="jakarta.persistence.jdbc.url" value="jdbc:mysql://localhost:3307/transportes_amartin?serverTimezone=UTC"/>
            <property name="jakarta.persistence.jdbc.user" value="root"/>
            <property name="jakarta.persistence.jdbc.password" value="root"/>

            <!-- Generación del esquema -->
            <property name="jakarta.persistence.schema-generation.database.action" value="drop-and-create"/>

            <!-- Configuración de Hibernate -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
            <property name="hibernate.highlight_sql" value="true"/>
            <property name="hibernate.globally_quoted_identifiers" value="true"/>
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQLDialect"/>
        </properties>
    </persistence-unit>

</persistence>